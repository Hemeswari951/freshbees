import 'package:flutter/material.dart';

// ── THIRAA color palette ────────────────────────────────────────────────────
// Shared across sidebar, header, all pages
class TColors {
  static const cream      = Color(0xFFFBF4ED); // main background
  static const cardBg     = Color(0xFFF3EBE0); // hover / card bg
  static const white      = Color(0xFFFFFFFF); // header bg, card bg
  static const black      = Color(0xFF1C1C1C); // active tile, buttons
  static const border     = Color(0xFFEDE4D8); // dividers, card borders
  static const brown      = Color(0xFF5C4A32); // normal nav text / icons
  static const brownLight = Color(0xFFA0896A); // muted text, section labels
  static const red        = Color(0xFFB94040); // logout, danger
}